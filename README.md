# become-geohotz
BUILD. BREAK. UNDERSTAND. 

# Table of contents 
- C programming
- Operating Systems (OSTEP)
- Embedded
    - ESP32
    - STM32

# Roadmap Özeti

```text
UBUNTU + TERMINAL + NEOVIM
            │
            ↓
        MINIMUM C
            │
            ↓
        1. ctodo
            │
      struct / pointer
       file handling
            │
            ↓
     2. Dynamic Array
            │
   malloc / realloc / GDB
            │
            ↓
      3. Allocator
            │
      memory internals
            │
            ↓
        4. Shell
            │
 fork / exec / pipe / signal
            │
            ↓
   5. Process Explorer
            │
    /proc / virtual memory
            │
            ↓
      6. Thread Pool
            │
   concurrency / synchronization
            │
            ↓
      7. HTTP Server
            │
       TCP / sockets
            │
            ↓
       8. Mini KV
            │
      integration project
            │
            ↓
════════ HARDWARE ════════
            │
            ↓
    9. Reaction Timer
            │
 GPIO / timer / interrupt
            │
            ↓
      10. Scheduler
            │
        FreeRTOS
            │
            ↓
      11. Sensor Hub
            │
 ADC / UART / I²C / datasheet
            │
            ↓
       12. RC Car
            │
 PWM / motors / Wi-Fi
            │
            ↓
     13. Autonomous Rover
            │
 encoder / PID / IMU / CV
            │
            ↓
        BIG MAKER
         PROJECTS
```
